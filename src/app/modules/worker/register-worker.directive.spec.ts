import { RegisterWorkerDirective } from './register-worker.directive';

describe('RegisterWorkerDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterWorkerDirective();
    expect(directive).toBeTruthy();
  });
});
