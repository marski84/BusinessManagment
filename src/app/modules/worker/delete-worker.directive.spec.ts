import { DeleteWorkerDirective } from './delete-worker.directive';

describe('DeleteWorkerDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteWorkerDirective();
    expect(directive).toBeTruthy();
  });
});
